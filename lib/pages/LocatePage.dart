import 'package:flutter/material.dart';

class LocatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('LocatePage'),
      ),
    );
  }
}
